server:
  port: ${PORT:10378}

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nhnacademy-be10-sajotuna/config-repo
          default-label: dev
          username: flashdragon
          password: ${CONFIG_TOKEN}
  application:
    name: config



management:
  endpoints:
    web:
      exposure:
        include: health,info,shutdown

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
